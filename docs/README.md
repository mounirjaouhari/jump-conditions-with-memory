# docs/

Documentation of the pipeline:

- [`ARCHITECTURE.md`](ARCHITECTURE.md) — the map: data flow, role of every
  module of `src/fading_memory/`, what each computation block establishes,
  and the design rules any contribution must respect;
- [`REPRODUCIBILITY.md`](REPRODUCIBILITY.md) — the procedure: environment,
  full and per-figure reproduction, the provenance contract of the
  manifests, numerical tolerances (what “reproduced” means), and expected
  runtimes.

Start with the top-level [`README.md`](../README.md).
